// Merchant model for payment gateway configurations
module.exports = {
  // Merchant fields:
  // _id: auto-generated
  // nickname: string (e.g., "Main Stripe Account", "PayPal Business")
  // gateway: string ("stripe", "paypal", "authorize")
  // credentials: object (encrypted gateway credentials)
  // isActive: boolean
  // createdAt: ISO date string
  // updatedAt: ISO date string
};
