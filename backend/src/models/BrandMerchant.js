// Brand-Merchant relationship model
module.exports = {
  // BrandMerchant fields:
  // _id: auto-generated
  // brandId: string (reference to Brand)
  // merchantId: string (reference to Merchant)
  // isDefault: boolean (default merchant for this brand)
  // createdAt: ISO date string
};
